---
inspecode:

  global:

  tools:

    stylelint:
    - config-file: 'inspecode-stylelintrc.json'
      options:
        --config-basedir: '.rocro'
      auto-fix: true
      thresholds: {num-issues: 28}
    - config-file: 'inspecode-stylelintrc.yaml'
      options:
        --config-basedir: '.rocro'
      # auto-fix: true
      thresholds: {num-issues: 28}
